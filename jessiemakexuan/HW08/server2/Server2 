<?php
    $arr = scandir("/proc/");
    //make a for loop to consume memory(sort array in ascending and descending order)
    for($x=0;$x<10000;$x++){
        asort($arr);
        arsort($arr);
    }
    print_r($arr);
    echo "server2";
?>
